import { useAppContext } from "../../context/app.provider";

const Home = () => {
    const {} = useAppContext()
    return (
        <div>
            Home Page
        </div>)
}

export default Home;